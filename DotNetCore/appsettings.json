{
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "EmployeeDBConnection": "server=(localdb)\\MSSQLLocalDB;database=EmployeeDB;Trusted_Connection=true"

    //All the below 3 settings specify the same thing, use Integrated Windows Authentication to connect to SQL Server instead of using SQL Server authentication.
    //"Trusted_Connection=True;" "Integrated",
    //"Security=SSPI;" "Integrated",
    //"Security=true;"
  }
  //1. "appsettings.json",
  //2. "appsettings." { "Environment" }
  //3. ".json",
  //4. "User",
  //5. "secrets" "Environment",
  //6. "variables" "Command-line",
  //7. "arguments": null,
  //"MyKey": "Value of MyKey from appsettings.json"
}
